<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Mal.XF.Infra.Controls;assembly=Mal.XF.Infra"
             xmlns:localisation="clr-namespace:Mal.XF.Infra.Localisation;assembly=Mal.XF.Infra"
             x:Class="Mal.XF.Infra.Pages.MasterMenu.MasterMenuPage"
             Title="Menu title">
    <StackLayout>
        <Label Text="{Binding Title}"  />
        <ListView ItemsSource="{Binding Tokens}"
                  SelectedItem="{Binding SelectedToken, Mode=TwoWay}">           
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal">
                            <controls:IconLabel Text="{Binding Icon}" 
                                                VerticalOptions="Center" />
                            <Label Text="{Binding Label, Converter={x:Static localisation:TranslationConverter.Instance}}" 
                                   LineBreakMode="TailTruncation"
                                   VerticalOptions="Center" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>